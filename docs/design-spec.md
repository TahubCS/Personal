# Systems Observatory — approved implementation contract

Astro static output; one React island for Context Compiler only. TraceTranscript.astro is rendered by the Astro page as a static sibling, never imported into React. Supo and CrowsNest use static architecture figures. No fabricated screenshots. Homepage target 400–600 narrative words excluding technical labels, navigation and transcript. Space Grotesk display, system sans body, IBM Plex Mono labels; self-host Latin subsets.

Graphite #111719, paper #F2EFE7, amber #E4B46B, steel #8DAAB8. Editorial 12-column desktop grid, 4-column mobile, maximum 1440px. Hero identity/contact precede optional interaction on mobile. Native scroll, accessible navigation, opaque sticky header. Source-linked project stories and three statically built case studies.

The Context trace is a deterministic local illustration: MCP client/stdio → repository-bound API authentication → query embedding → hybrid indexed retrieval → returned context. Index preparation is separate. Invalid-key and empty-result branches, pause/resume, direct step controls, restart, complete transcript. No live external API requests. No autoplay on load; pause offscreen/hidden. Reduced motion uses manual stepping and no translations.

Milestones: A foundation/tokens; B navigation/hero; C index; D trace; E stories/routes; F contact/accessibility/responsive polish. Run checks and inspect rendering before each milestone commit. Performance targets: compressed HTML 60KB, CSS 30KB, initial JS 20KB, hydrated JS 120KB, fonts 160KB, initial transfer 650KB; LCP 2.5s, INP 200ms, CLS .1. Report measured deviations. Available automated/browser checks must run; unavailable browsers, physical-device Safari and screen readers remain manual release checks.
